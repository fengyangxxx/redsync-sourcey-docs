# Frantic #33 Workflow Candidate Execution Record

This record supersedes earlier worker handoffs for the workflow candidate.
It is claim-neutral and carries no publication or delivery approval.

## Current Platform State

- Claim state: `unclaimed`; no claim ID or deadline is bound to these static bytes.
- `work_status`, slot availability, and live agent state are unresolved until a
  fresh pre-claim board snapshot is captured.

## Fixed Roles

- Gauss (`019f65c3-203f-7990-8feb-3cc1ee98d86c`, xhigh) owns implementation,
  local debugging, deterministic tests, and local evidence preparation.
- Dirac (`019f65c3-29ec-7181-9277-b251442a3250`, xhigh) is the independent QA
  gatekeeper. Only a fresh QA decision for the exact reviewed bytes can
  authorize the next governed stage.

## Candidate Lineage

- Docs publication commit: `bc5585dae317d2fcbd48b3774ba10a27f2e585d6`.
  It becomes the immutable public Sourcey documentation artifact only after it
  is fast-forwarded to public `main`, Read the Docs reports that exact commit,
  and anonymous readback matches its generated bytes.
- Current candidate: the workflow/receipt candidate containing this record; its direct
  parent is `bc5585dae317d2fcbd48b3774ba10a27f2e585d6`.
- The candidate is workflow/receipt tooling only. It is separate from the
  docs publication commit and is never itself a Read the Docs deployment input.
- Pinned target: `go-redsync/redsync@79f6ba24a8bf41f35141de700d410a06bb27622f`.
- Public-host thesis: claimant-authored, project-named ReadTheDocs community
  documentation. It is not target-owned or official.
- PR #245 is an optional open and unmerged link proposal. It is not adoption,
  endorsement, or maintainer acceptance.

## Exact Outbound Plan

- Public repository: `https://github.com/fengyangxxx/redsync-sourcey-docs`.
- Destination ref: `refs/heads/fix/frantic33-governed-receipt-v11`.
- Expected pre-push state: branch absent.
- Workflow: `.github/workflows/validate-sourcey-adoption.yml`.
- Dispatch ref: `fix/frantic33-governed-receipt-v11`.
- Final delivery authorization: `false`.

## Required Governed Sequence

1. Capture a fresh live board snapshot, obtain a new claim, and record its exact
   claim ID and deadline before any governed dispatch or delivery preparation.
2. Obtain Dirac outbound-publication QA for the exact D and W commits.
3. Fast-forward D to public `main`, wait for Read the Docs to report D, and
   require anonymous byte-identical narrative and API readback.
4. Fresh-check that the W destination branch is absent, publish only the
   reviewed workflow/receipt tooling bytes, and read back W byte-identically.
5. Dispatch the governed workflow exactly once on the recorded W ref and require
   clean raw output, a complete receipt-status audit, byte-exact archive
   reconstruction, and successful root receipt verification.
6. Materialize immutable evidence for only that successful run, then obtain a
   separate full Dirac review of the exact payload.
7. Keep delivery authorization false until the fresh claim is live and unexpired,
   immutable final artifacts exist, and separate guarded-submit QA passes.

The candidate is not authorized for outbound use until the applicable fresh
Dirac QA record ends in `QA_DECISION: PASS`.
